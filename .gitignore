# Editor directories and files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# OS generated files
Thumbs.db
Desktop.ini

# Logs
*.log
npm-debug.log*

# Temporary files
*.tmp
*.temp
.cache/

# Build artifacts (for future if we add a build process)
dist/
build/
out/

# Node modules (if we add npm in the future)
node_modules/

# Environment variables
.env
.env.local
.env.*.local

# Test coverage (if we add tests)
coverage/
.nyc_output/

# Future asset sources (keep compiled assets, ignore sources)
assets/src/
*.psd
*.ai
*.sketch
*.fig

# Backup files
*.bak
*.backup
index.html.backup

# Documentation build (if we add docs generation)
docs/_build/
docs/.doctrees/

# Package manager lock files (personal preference to ignore for simple projects)
# Uncomment if you want to track them:
# package-lock.json
# yarn.lock
# pnpm-lock.yaml
